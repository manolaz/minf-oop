import observer.IObserver;

public class Logger implements IObserver{

    public void update(AccountStatus state) {
        data.setStatus(state);
    }

    public void login() {
        if (condition) {
            changeStatus(AccountStatus.INVALID_ID)
        }
        else if

    }

    public boolean isValidIp(){

    }


    private valid 


    @Override
    public String toString() {
        return "{" +
            "}";
    }
    
}